# react-native-image-to-pdf declara peer react-native@^0.41.2 (desactualizado)
# pero funciona correctamente con RN 0.81. Esta bandera permite que EAS Build
# (que usa npm ci en modo estricto) instale el paquete sin fallar.
legacy-peer-deps=true
